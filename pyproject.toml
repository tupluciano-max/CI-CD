# pyproject.toml
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "mi-calculadora"
version = "0.1.0"
description = "Proyecto de ejemplo para clase de CI/CD"
requires-python = ">=3.9"
